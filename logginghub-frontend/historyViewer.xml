<container>

    <environmentController />

    <environment name="local" showHistoryTab="true">
        <hub name="local" host="localhost" port="58770" />
    </environment>

    <mainFrame name="LoggingHub History Viewer">
        <menuBar />
        <tabbedPane>
            <historyView name='History view' environmentRef='local' /> 
        </tabbedPane>
    </mainFrame>

</container>